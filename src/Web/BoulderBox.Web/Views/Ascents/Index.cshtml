@model IEnumerable<BoulderBox.Web.ViewModels.Ascents.AscentViewModel>

@{
    ViewData["Title"] = "Ascents";
}

<div class="row">
    <h2 class="ml-2">Ascents</h2>
</div>
<hr class="bg-secondary" />

<div class="table-responsive">
    <table class="table table-borderless table-hover">
        <thead class="bg-primary text-light">
            <tr>
                <th>User</th>
                <th>Date</th>
                <th>Style</th>
                <th>Boulder</th>
                <th>Comment</th>
                <th>Stars</th>
            </tr>
        </thead>
        <tbody class="table-light">

            @if (!Model.Any())
            {
                <tr>
                    <td>No ascents</td>
                    <td>-</td>
                    <td>-</td>
                    <td>-</td>
                    <td>-</td>
                </tr>
            }

            @foreach (var ascent in Model)
            {
                var styleBadge = "badge badge-";

                if (ascent.StyleShortText == "OS")
                {
                    styleBadge += "info";
                }
                else if (ascent.StyleShortText == "FL")
                {
                    styleBadge += "warning";
                }
                else if (ascent.StyleShortText == "RP")
                {
                    styleBadge += "danger";
                }

                <tr>
                    <td><a asp-area="" asp-controller="Users" asp-action="Details" asp-route-id="@ascent.ApplicationUserId" class="text-dark">@ascent.ApplicationUserUsername</a></td>
                    <td>@ascent.Date.ToString("dd/MM/yyyy")</td>
                    <td><span class="@styleBadge">@ascent.StyleShortText</span></td>
                    <td><a asp-area="" asp-controller="Boulders" asp-action="Details" asp-route-id="@ascent.BoulderId" class="text-dark">@ascent.BoulderName</a> (@ascent.GradeText)</td>
                    <td>@(ascent.Comment ?? "-")</td>
                    @if (ascent.Stars == null)
                    {
                        <td>-</td>
                    }
                    else
                    {
                        <td>@ascent.Stars</td>
                    }
                </tr>
            }
        </tbody>
    </table>
</div>
