@model BoulderBox.Web.ViewModels.Common.PaginationViewModel

<nav>
    <ul class="pagination">
        <li class="page-item @(!Model.HasPrevoius ? "disabled" : string.Empty)">
            <a class="page-link" asp-route-pageId="@(Model.FirstPage)"><i class="fas fa-angle-double-left"></i></a>
        </li>
        <li class="page-item @(!Model.HasPrevoius ? "disabled" : string.Empty)">
            <a class="page-link" asp-route-pageId="@(Model.PrevoiusPage)"><i class="fas fa-angle-left"></i></a>
        </li>

        @for (int i = Model.CurrentPage - 2; i <= Model.CurrentPage + 2; i++)
        {
            if (i >= 1 && i <= this.Model.LastPage)
            {
                <li class="page-item @(i == Model.CurrentPage ? "active" : string.Empty)">
                    <a class="page-link" asp-route-pageId="@i">@i</a>
                </li>
            }
        }

        <li class="page-item @(!Model.HasNext ? "disabled" : string.Empty)">
            <a class="page-link" asp-route-pageId="@(Model.NextPage)"><i class="fas fa-angle-right"></i></a>
        </li>
        <li class="page-item @(!Model.HasNext ? "disabled" : string.Empty)">
            <a class="page-link" asp-route-pageId="@(Model.LastPage)"><i class="fas fa-angle-double-right"></i></a>
        </li>
    </ul>
</nav>